machine:
  timezone: Asia/Tokyo
deployment:
  production:
    branch: master
    commands:
      - git clone -b gh-pages git@github.com:kawausokun/wcl-tooltip.git
      - cd wcl-tooltip
      - npm run build
      - git commim -a -m 'Update demo site via CI'
      - git push origin master
